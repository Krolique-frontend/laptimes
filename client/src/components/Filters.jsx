import React from 'react';
import f from './Filters.module.css';

export const Filters = ({filters}) => {

    return(
        <div className={f.achtung}>
            Фича в стадии разработки. Пардоньте за временные трудности
        </div>
    );
};